# Inventário de EXEMPLO (placeholder) — este vai pro repositório.
# Mantém o mesmo modelo do `inventory.producao.ini`, só que com valores fictícios.
# Pra rodar de verdade, preencha os dados reais no `inventory.producao.ini`
# (que está no .gitignore).

[all]
korp-server ansible_host=IP_DA_EC2 ansible_user=ubuntu ansible_ssh_private_key_file=/caminho/ate/chave.pem

[all:vars]
projeto_dir=/opt/korp-desafio-devops
