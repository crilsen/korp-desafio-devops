# arquivos de inventário com dados reais (IP, usuário, chave) não sobem pro repo
ansible/inventory.producao.ini
inventory.producao.ini

# chaves privadas
*.pem
*.key

# configurações locais de ambiente
*.conf

# isos e arquivos grandes baixados acidentalmente no repo
*.iso

# secrets: senha do grafana e hash do basic auth do prometheus
.env
nginx/.htpasswd

# docs pessoais (checklist, guia de entrevista e o PDF do desafio) — não sobem pro repo
docs/

# binários e artefatos locais
http-server/http-server

# arquivos de sistema do macOS
.DS_Store
**/.DS_Store

# terraform: estado, cache do provider e tfvars com dados reais
terraform/.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
